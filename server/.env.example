SECRET_KEY=your-secret-key-here

# Comma-separated list of hosts Django accepts in production (DEBUG=False).
# Leave unset for local dev - DEBUG mode accepts any host automatically.
# Example:
#   ALLOWED_HOSTS=localhost,127.0.0.1,backend,quizscendence.example.com
#ALLOWED_HOSTS=

DB_NAME=trans_db
DB_USER=trans_user
DB_PASSWORD=your_password_here
DB_HOST=127.0.0.1
DB_PORT=5432

#USE_POSTGRES=1
